<div class="inline text-gray-600 hover:text-gray-800 dark:text-gray-400 dark:hover:text-gray-50 font-normal"
     title="{{ $title }}" alt="{{ $title }}">
    <x-icon solid class="inline cursor-pointer" name="{{ $icon }}" {{ $attributes }} />
</div>
